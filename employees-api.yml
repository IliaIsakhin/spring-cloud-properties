server:
  port: 9092
  
instance:
  appname:
    employees-api

management:
  endpoints:
    web:
      exposure:
        include: '*'

feign:
  enabled: true
  client:
    config:
      workspaces-api:
        connectTimeout: 2000
        readTimeout: 2000
        loggerLevel: full
